
<!--<p>Hello <%#= @resource.email %>!</p>

<p> Requested a link to change your password. You can do this through the link below.</p>

<p><%#= link_to 'Change my password', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %></p>

<p>If you didn't request this, please ignore this email.</p>
<p>Your password won't change until you access the link above and create a new one.</p>
 -->


Forgot password

Hi, We've received a request to reset your Fiverr password.

To initiate the process, please click the following link:
<p><%= link_to 'Change my password', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %></p>

If clicking the link above does not work, copy and paste the URL in a new browser window. 